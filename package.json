{
  "name": "vinylwraptoronto",
  "type": "module",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "astro": "astro",
    "check": "node scripts/sweep.mjs",
    "deploy": "astro build && node scripts/sweep.mjs && wrangler deploy"
  },
  "dependencies": {
    "@astrojs/cloudflare": "^12.6.10",
    "astro": "^5.14.1"
  },
  "devDependencies": {
    "wrangler": "^4.59.2"
  }
}
